namespace Still.Enum.CompareType
{
    public enum CompareType
    {
        Equal,      // 等しい (==)
        Less,       // より小さい (<)
        Greater,    // より大きい (>)
        NotEqual,   // 等しくない (!=)
        LessEqual,  // 以下 (<=)
        GreaterEqual // 以上 (>=)
    }
}